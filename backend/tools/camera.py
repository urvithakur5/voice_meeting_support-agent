def check_camera():
    return {
        "tool": "check_camera",
        "status": "failure",
        "device": "Default Camera",
        "device_connected": True,
        "permission": True,
        "working": False,
        "message": "Camera is connected but not working."
    }